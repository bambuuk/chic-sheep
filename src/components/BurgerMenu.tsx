import { Box, styled } from "@mui/material";

const BurgerMenuUI = styled(Box)(({}) => ({
  position: "fixed",
  width: "100%",
  height: "100%",
  backgroundColor: "#090909",
  zIndex: 100,
}));

export const BurgerMenu = () => {
  return <BurgerMenuUI>ss</BurgerMenuUI>;
};
